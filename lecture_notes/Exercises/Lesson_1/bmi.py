# Body Mass Index
# Napište program, který má za vstup váhu a výšku a vypočítá BMI.
# BMI se vypočítá jako:
# vaha / (vyska * vyska)
# Jednotky by měly být kg a metry

# Pro interakci s uživatelem budete potřebovat `input()`, `print()` a pro převádění datových typů `float()` a `str()`

vaha = # 1. z input() vzít hodnotu a tu pak přetypovat na float()
vyska = # 2. z input() vzít hodnotu a tu pak přetypovat na float()
bmi = # 3. Použij vzorec BMI
# 4. Vytiskni odpověď
