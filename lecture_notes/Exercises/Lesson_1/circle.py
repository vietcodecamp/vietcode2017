# Circle
# Napište program, který má za vstup poloměr kruhu a vypočítá jeho obvod, obsah a objem kruhu o daném poloměru.
# Pro interakci s uživatelem budete potřebovat `input()`, `print()` a pro převádění datových typů `float()` a `str()`
# Nápověda:
#  - použijte proměnnou `PI` a nastavte si jí na `3.14`, abyste nemuseli psát číslo `3.14` na více místech.
#  - Objem koule se vypočítá jako V = 4/3 * π * r^3  (`^3`: znamená na třetí - v pythonu `** 3`)

PI = 3.14 # definování konstanty - proměnná, která se nemění
# 1. získat poloměr od uživatele.

# 2. Vypočítat obvod kruhu

# 3. Vypočítat obsah kruhu

# 4. Vypočítat objem koule

# 5. Vytisknutí všech hodnot.
