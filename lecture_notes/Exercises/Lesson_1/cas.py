# Prevod minut na hodiny a minuty
# Napište program, který má za vstup počet minut a vytiskne počet hodin a minut.
# Pro interakci s uživatelem budete potřebovat `input()`, `print()` a pro převádění datových typů `int()` a `str()`
# Nápověda:
#  - pro celočíselné dělení použijte `//`
#  - pro získání zbytku po dělení použijte modulo `%` - příklad: 5%3 vrací 2 - Zbytek po dělení čísla 5 číslem 3 je 2.

# 0. definování konstanty počtu minut v hodině - není nutné, ale je to lepší. Kód je čitelnější
POCET_MINUT_V_HODINE = 60

# 1. vypočítat počet hodin z minut (Hint: použít celočíselné dělení `//`)
# 2. vypočítat počet počet zbylých minut (Hint: zbytek po dělení - modulo `%`)
# 3. Tisk vypočtených proměnných
